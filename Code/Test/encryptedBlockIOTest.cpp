#include <encryptedblockIO.h>

int main(int argc, char** argv) {
    encryptedFileread reader;
    encryptedFilewrite writer;

    // read and write files of many different lengths, generated randomly from /dev/urandom
    
    // Fix: add test code with a fixed key

    return 0;
}
