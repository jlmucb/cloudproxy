Todo
====

Enable measure hashes and add to KeyNegoServer db  
Enable 2048 bit keys, 
Check timing attack.
Check for leaks buffer overflows
Measurement race when starting app/vm
tcService hang after init

get rid of hardcoded paths

keyNegoServer: emergency key recovery program, revocation
            file key escrow and update
Centralized keyNegoServer

tcLaunch: replace hardcoded path in tcLaunch with designated temp area


KVM:
    initram for kvm
    Disable any functions-migrate, etc
    Disable save state for OS
    Minimize Linux and configure policy
fix pid hack

KVM Guest:
    Minimize Linux
    Network save of key files, etc.

Hypervisor
    Management partition
        schedule, start vm's, montoring, update
    VM update and keys

Not required (if configuration is perfect and initram is measured) but disireable:
measure tcService.

